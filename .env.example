# Leave MONGODB_URI empty to run the seeded local review mode.
MONGODB_URI=
MONGODB_DB=forecourt
OWNER_NAME=Edwin
OUTLET_NAME=Swanith Fuels

# Authentication is intentionally disabled in local review mode.
# Configure the deployment's owner session and recovery secrets before production.
OWNER_EMAIL=owner@example.com
OWNER_SESSION_SECRET=replace-with-a-long-random-secret
